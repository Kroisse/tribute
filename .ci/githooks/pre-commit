#!/bin/sh

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

echo "Running pre-commit checks..."

git diff --cached --check
"$SCRIPT_DIR/../../.ci/lint.sh" --quick

echo "✅ All pre-commit checks passed!"
